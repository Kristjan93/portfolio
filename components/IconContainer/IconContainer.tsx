import styled from 'styled-components'

/**
 * Svg elements behave in a special way.  For ease of use make them normal.
 */
export const IconContainer = styled.div`
  display: flex;
  flex-direction: column;
`
